# 虎牙纯净直播 | 去广告·深色·拾取元素 —— GreasyFork 发布文案

## ① 脚本名称（@name）

`虎牙纯净直播 | 去广告·深色·拾取元素`

## ② 脚本描述栏（@description，一行式）

```
①纯净看播：主播位横幅/侧栏广告/游戏售卖组件/主播背景推广图一键隐藏，只留直播画面和聊天区；②布局兜底(默认开)：画面被顶出视口自动回收大块广告，改版也不怕；③视口锁定(实验性)：播放器+聊天区钉死视口，广告再也推不动画面；④🎯点谁删谁：拾取模式直接点掉漏网广告；⑤深色背景(可开关)+可拖动齿轮按钮，清爽不挡画面
```

## ③ 详细介绍栏（Markdown）

```markdown
# 🧹 虎牙纯净直播 | 去广告·深色·拾取元素

> 看直播，只看直播。广告？点谁删谁。

## ✨ 特色

1. 🚫 **纯净去广告**：一键隐藏虎牙直播间广告——主播自设背景推广图（常把播放器顶到一屏以下的大图）、播放器标题栏的主播位横幅（「点亮进度」等推广轮播）、右侧栏广告位、游戏售卖组件，全部消失。只删广告节点，播放器与聊天区完好无损。
2. 🛡️ **布局兜底**（默认开）：不依赖任何类名，虎牙改版换类名、上新广告活动，只要它把直播画面顶出视口，都会被自动回收，画面永远回得来。
3. 📌 **视口锁定模式**（默认关·实验性）：播放器 + 聊天区钉死当前窗口、页面不可滚动，任何广告都再也推不动画面。
4. 🎯 **点谁删谁（拾取元素）**：又出新广告？不会写 CSS 也没关系——点 ⚙ →「🎯 拾取元素」→ 直接点那个广告，自动生成隐藏规则、立即生效。左键点元素、Shift+左键点父级（点小图删整个广告块）、Esc 或右键结束。
5. 🌙 **深色护眼背景**（可开关）：夜间看播不刺眼。
6. ⚙ **清爽面板**：齿轮按钮带呼吸光晕、可拖到任意位置；面板鼠标移开即关、5 秒无操作自动收起，绝不挡画面。

## 🛠 使用

- 安装后打开任意虎牙直播间（huya.com/房间名）即生效，右上角出现旋转 ⚙ 即安装成功。
- 漏网广告：⚙ →「🎯 拾取元素」→ 点它。
- 误伤恢复：⚙ → 自定义规则 → 删掉对应行 → 点「应用规则」。

## 📌 说明

- 仅作用于直播间页面，虎牙首页/分类页不干预。
- 可与斗鱼版《斗鱼去广告 + 自动网页全屏 (性能优化版)》同时安装，互不冲突。
- 本脚本开源见 GitHub：github.com/louhongwu/douyu-clean
```

## ④ 更新说明栏（当前版本 v0.2）

```
v0.2（背景图+双保险）：
①新增隐藏「主播自设背景推广图」(把播放器顶到一屏以下的大图)，进房即见画面；
②移植斗鱼版「布局兜底」(默认开)：不依赖类名，画面被顶出视口自动回收大块广告；
③移植「视口锁定模式」(实验性·默认关)：播放器+聊天区钉死视口、页面不可滚动，广告再也推不动画面。

v0.1：首发——去广告(主播位横幅/侧栏广告/游戏售卖组件) + 拾取元素点选去广告 + 深色背景 + 可拖动面板。
```

---

## 发布步骤备忘

1. GreasyFork → 上传脚本 → 代码框粘贴 `huya-clean-v0.2.user.js` 全部内容（以 `// ==UserScript==` 开头，无 BOM）。
2. 首次发布「同步」栏不填；若已发布过，用脚本页链接同步。
3. 描述栏粘贴上面的介绍 Markdown。
4. 更新说明栏粘贴 v0.2 文案。
5. 发布后把脚本页链接发给作者，可代填 `@updateURL`/`@downloadURL` 实现自动更新。
