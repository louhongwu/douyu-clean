# 斗鱼去广告 + 自动网页全屏 (性能优化版)

> 白名单式去广告 · 深色护眼 · 真实数据 · 自动全屏 · 弹幕增强，一个脚本搞定斗鱼直播间。

---

## 📦 功能亮点

1. **白名单式去广告**：只保留「直播主画面 + 弹幕栏」，其余结构（导航、榜单、活动、悬浮窗、推广）一律视为广告隐藏。广告清得干净，不留空容器，直播画面始终居中，不用满屏找画面。
2. **拾取元素（可视化点选去广告）**：不会写 CSS 选择器？点「🎯 拾取元素」后直接点漏掉的广告，自动生成隐藏规则并立即生效（左键=加入规则，Shift+左键=选整个广告块，Esc 或右键=结束）；播放器/弹幕栏有误选保护。
3. **布局级自动兜底**（默认开启，可开关）：不依赖任何斗鱼类名，按「占位尺寸 + 位置」自动回收把直播画面顶出视口的大块空容器。就算斗鱼下次改版换了全部 CSS 类名、或者上线新活动横幅，直播画面也能自动回到眼前。
4. **视口锁定模式**（默认关闭 · 实验性，可开关）：播放器 + 弹幕栏钉死在当前窗口，页面不可滚动，任何新广告都再也推不动画面；网页全屏时播放器自动铺满、弹幕栏隐藏。
5. **深色护眼背景**：夜间看播不刺眼，沉浸感拉满。
6. **真实数据面板**：今日活跃 / 已播时长 / 弹幕人数 / 礼物价值 / 贵宾 / 粉丝，图标+数字一目了然，鼠标悬浮显示完整说明（DouyuEx 同款交互）。主播换过房间号也能自动识别（如 39939 → 2421040），主播改直播间标题也不影响识别（v13.24 起按标题尾段提取主播名）；每 5 分钟自动刷新，另有手动刷新按钮兜底，数据接口异常时 15 秒快速重试。
7. **自动网页全屏**（可开关）：进房即享大屏，弹幕栏依然保留，可随时在 ⚙ 面板关闭。
8. **可拖动齿轮按钮 + 面板自动收起**：⚙ 按钮带呼吸光晕、可拖到任意位置；面板鼠标移开自动关闭，5 秒无操作也会收起，绝不挡画面。
9. **弹幕无限收藏 + 收藏搜索**：官方云收藏 50 条上限不再是天花板，超出自动转存本地实现无限收藏，收藏弹窗支持关键词搜索。
10. **图片弹幕**：与 DouyuEx 同款标记互通，图片弹幕双方可见。
11. **自动抢礼物红包**（默认关闭）：检测到房间礼物红包，开抢前自动出手，连抢 3 次，无需粉丝牌门槛。
12. **半自动抢宝箱**（默认关闭 · 实验性）：订阅页面宝箱推送，到点自动领取。
13. **检查更新 + 更新说明**：⚙ 面板一键检查 GreasyFork 新版本，并可查看最近版本更新说明。

---

## 🛠 使用说明

- 安装后进入任意斗鱼直播间即生效（首页 / 分类页仅做轻量去广告，不影响浏览）。
- 点右上角 ⚙ 齿轮按钮打开设置面板，按需开关各功能。
- 签到功能：斗鱼官方已下线直播间签到，按钮已置灰并提示。
- 需登录斗鱼账号的功能：弹幕收藏 / 图片弹幕 / 抢红包 / 抢宝箱。
- 图片弹幕需发送方与接收方都安装支持该标记的脚本（本脚本或 DouyuEx）。
- 若遇到新活动把画面顶走：保持默认设置即可（自动兜底会拉回画面）；追求绝对稳定可开启「视口锁定模式」。
- 遇到漏掉的广告：⚙ 面板 → 自定义规则 →「🎯 拾取元素」，直接点它即可。

---

## 📝 更新日志

- **v13.25（进房卡顿优化）**：打开直播间不再卡一下——三处进房瞬间同步阻塞主线程的代码全部让出首屏渲染窗口：①空容器回收延迟 6 秒启动，观察回调节流 300ms 合并扫描（不再对 React 渲染的每个新增节点做子树查询）；②收藏搜索观察器 500ms 防抖（不再每次 DOM 变化都全文档查询）；③布局兜底首扫延迟 10 秒且页面加载中不扫描（不再与斗鱼首屏渲染抢主线程）。
- **v13.24（数据修复 + 性能版）**：修复部分直播间数据全为 `--`——斗鱼页面标题第一段是主播可自定义的「直播间标题」（如「影之刃零预售」）而非主播名，主播改标题后真实房号解析失效；现在按标题尾段/第二段提取主播名，suggest 结果按「精确→包含」匹配；弹幕/礼物缺失即触发房号解析，数据接口返回异常内容也快速重试。性能优化：贵宾通道改用 TextDecoder 只解码贵宾消息（大直播间每秒大量弹幕不再频繁创建 FileReader）、修复新旧 WebSocket 连接竞态；面板窄窗口不再溢出；toast 单例化不叠屏；后台心跳刷新已播时长；收藏未登录不再谎报成功。
- **v13.23（拾取元素）**：新增「🎯 拾取元素」——自定义规则区点一下，直接点漏掉的广告，自动生成 CSS 选择器并立即隐藏（左键=加入规则，Shift+左键=选整个广告块的父级，Esc 或右键=结束）；不会写选择器也能手动去广告，播放器/弹幕栏有误选保护。
- **v13.22（抗改版加固）**：新增两道保险（⚙ 面板均可开关）——①「布局级自动兜底」（默认开）：不依赖斗鱼类名，每 2 秒按占位尺寸+位置自动回收把播放器顶出视口的大块容器，画面已在视口上部时不打扰，斗鱼改版换类名或上新横幅也拉得回画面；②「视口锁定模式」（默认关 · 实验性）：播放器+弹幕栏钉死当前窗口、页面不可滚动，网页全屏时播放器铺满+弹幕栏隐藏，从根上杜绝画面被顶走。
- **v13.21（活动直播间修复）**：修复部分活动直播间（如 51111）画面被顶出视口——白名单重构时遗漏的 `.wm-general` 大横幅容器规则已补回（活动页大横幅+主播推荐模块固定高度 1100px+，把播放器推到一屏以下），现在进房即见画面，无需滚动。
- **v13.20**：信息条改版（学 DouyuEx）——6 项数据改为「图标+数字」简洁显示，鼠标悬浮才显示完整说明（如「今日累计活跃人数:4971」，数值实时同步）；自定义隐藏规则区新增小白教程（怎么找广告元素的 class/id、三种写法示例、误伤如何恢复）。
- **v13.19**：新增「更新说明」版块——⚙ 面板一键查看最近版本更新内容（点击任意处或 12 秒自动关闭）。
- **v13.18（检查更新修复）**：修复「检查更新失败：未获取到版本信息」——版本号正则写错（`\s` 误写成字面反斜杠，永远匹配不到 `@version`），已改正并放宽空白匹配；`@connect` 补上 `update.greasyfork.org` 授权（此前会触发 Tampermonkey 跨域拦截）；检查更新升级为双通道：主源 meta.js 失败时自动改用 GreasyFork 脚本页 `data-script-version` 兜底，错误提示区分地址异常/网络异常/超时。
- **v13.17（稳定性版）**：修复 SPA 切房数据污染——新增统一房间生命周期（切房时销毁全部定时器/WebSocket/红包宝箱订阅/XHR 并清空旧房状态，不再串房显示旧数据）；修复红包与宝箱的 var 闭包 Bug（多个红包/宝箱同时出现不再互相覆盖）；红包单次最多重试 5 次并带切房校验，不再无限刷请求；宝箱关闭时真正解除 tslist 订阅，反复开关不累积监听；贵宾通道支持主动停止（切房不触发假重连）；空容器回收 60 秒后降为低频扫描，长期持续去广告。
- **v13.16**：健壮性大修——①抢红包/抢宝箱未登录时直接提示并回弹开关，不再静默空转；②贵宾数据通道后台标签页暂停重连计时，切回前台立即恢复；③自动全屏增加语义兜底，前端类名改版失效也能按「全屏」按钮找得到；④房号解析兜底加 8 秒超时，搜索卡住不再拖住数据面板；⑤收藏搜索由轮询改为 MutationObserver，弹窗出现即注入；⑥图片弹幕图片懒加载，不拖慢弹幕滚动。
- **v13.15**：接入 GreasyFork 真实更新地址（脚本 ID 581908），老用户可直接检查更新。
- **v13.13**：融合 DouyuEx 功能——弹幕无限收藏 + 收藏搜索、图片弹幕、自动抢礼物红包、半自动抢宝箱、已播时长、GreasyFork 检查更新；新增 ⚙ 面板开关与「检查更新」按钮。
- **v13.12**：修复旧房间号自动识别（主播换房后数据全 0，自动按主播名搜索真实房号并重拉数据）。
- **v13.11**：⚙ 面板优化——齿轮可拖动、5 秒无操作自动收起、鼠标移开立即关闭；信息条对齐弹幕栏、全屏时自动隐藏。

---

## ✅ 更新地址已配置

脚本头 `@updateURL` / `@downloadURL` 与代码内 `UPDATE_META_URL` 均已填入 GreasyFork 真实地址（脚本 ID 581908），老用户安装新版本后即可自动收到后续更新提醒。

---

## 📌 兼容性

- 浏览器：Tampermonkey（Chrome / Edge 等 Chromium 内核）推荐，其他管理器未逐一测试。
- 域名：`www.douyu.com` / `douyu.com` 直播间、首页、分类页。